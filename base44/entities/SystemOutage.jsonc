{
  "name": "SystemOutage",
  "type": "object",
  "properties": {
    "component": {
      "type": "string",
      "description": "Component name affected"
    },
    "issue_key": {
      "type": "string",
      "description": "Stable scan or manually reported issue identity"
    },
    "source": {
      "type": "string",
      "description": "Origin of the issue, such as full_app_scan or manual"
    },
    "last_seen_at": {
      "type": "string",
      "format": "date-time",
      "description": "Most recent scan that still detected the issue"
    },
    "occurrence_count": {
      "type": "integer",
      "default": 1,
      "description": "Number of scans or reports that detected this issue"
    },
    "severity": {
      "type": "string",
      "enum": [
        "degraded",
        "outage",
        "maintenance"
      ],
      "default": "outage",
      "description": "Severity level"
    },
    "title": {
      "type": "string",
      "description": "Short title of the outage"
    },
    "description": {
      "type": "string",
      "description": "Detailed description of the issue"
    },
    "reported_by": {
      "type": "string",
      "description": "Admin who reported the outage"
    },
    "resolved_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the outage was resolved (null if still active)"
    },
    "resolved_by": {
      "type": "string",
      "description": "Admin who marked it resolved"
    }
  },
  "required": [
    "component",
    "severity",
    "title"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": null,
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}