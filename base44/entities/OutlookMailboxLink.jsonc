{
  "name": "OutlookMailboxLink",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Pathfinder user ID"
    },
    "pathfinder_email": {
      "type": "string",
      "description": "Pathfinder account login email"
    },
    "outlook_email": {
      "type": "string",
      "description": "Connected Outlook/Microsoft 365 mailbox email"
    },
    "outlook_display_name": {
      "type": "string",
      "description": "Microsoft mailbox display name"
    },
    "microsoft_user_id": {
      "type": "string",
      "description": "Microsoft Graph user object ID"
    },
    "connected": {
      "type": "boolean",
      "default": true
    },
    "connected_at": {
      "type": "string",
      "format": "date-time"
    },
    "last_verified_at": {
      "type": "string",
      "format": "date-time"
    },
    "disconnected_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "user_id",
    "pathfinder_email",
    "outlook_email"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}