{
  "name": "CadCounter",
  "type": "object",
  "properties": {
    "counter_key": {
      "type": "string",
      "description": "Unique sequence key"
    },
    "last_number": {
      "type": "number",
      "default": 0,
      "description": "Last issued CAD sequence number"
    },
    "ingestion_lock_token": {
      "type": "string",
      "description": "Short-lived ownership token preventing concurrent GRAC ingestion"
    },
    "ingestion_locked_until": {
      "type": "string",
      "format": "date-time",
      "description": "Expiration time for the GRAC ingestion lease"
    }
  },
  "required": [
    "counter_key",
    "last_number"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}