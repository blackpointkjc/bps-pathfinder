{
  "name": "CompanyExpense",
  "type": "object",
  "properties": {
    "expense_date": {
      "type": "string",
      "format": "date",
      "description": "Date the bill or expense was incurred"
    },
    "vendor": {
      "type": "string",
      "description": "Vendor or payee"
    },
    "category": {
      "type": "string",
      "enum": [
        "rent",
        "utilities",
        "insurance",
        "software",
        "equipment",
        "supplies",
        "fuel",
        "maintenance",
        "professional_services",
        "taxes_fees",
        "marketing",
        "other"
      ]
    },
    "description": {
      "type": "string",
      "description": "Bill or expense description"
    },
    "amount": {
      "type": "number",
      "description": "Total company expense"
    },
    "site_name": {
      "type": "string",
      "description": "Optional site or cost center"
    },
    "reference_number": {
      "type": "string",
      "description": "Invoice, bill, or reference number"
    },
    "status": {
      "type": "string",
      "enum": [
        "unpaid",
        "paid",
        "void"
      ],
      "default": "paid"
    },
    "due_date": {
      "type": "string",
      "format": "date"
    },
    "paid_date": {
      "type": "string",
      "format": "date"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "expense_date",
    "vendor",
    "category",
    "description",
    "amount",
    "status"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}